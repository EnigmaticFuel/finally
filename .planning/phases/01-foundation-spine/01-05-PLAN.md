---
phase: 01-foundation-spine
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - db/finally.db
autonomous: false
requirements: [SETUP-06, CORE-04]

estimate:
  tokens: 35000
  raw_tokens: 23000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The tracked db/finally.db carries the standard fresh seed: one profile with 10000.0 cash, exactly ten watchlist tickers matching DEFAULT_TICKERS, exactly one portfolio snapshot, and zero rows in positions, trades and chat_messages"
    - "db/finally.db remains tracked in git after the rewrite — the file is replaced in place, never untracked"
    - "The rewritten database is produced by calling this phase's own apply_schema and seed_fresh, not by hand-editing SQL, so the committed artifact and the lazy-init path cannot disagree"
    - "A fresh clone of the repository shows the first-launch experience the product specification promises: $10,000 cash and the ten default tickers"
    - "The full backend test suite shows no new failures relative to the pre-phase baseline"
    - "A baseline comparison that exactly equals the pre-phase failure set passes the gate; one additional failing test node fails it"
    - "An empty pre-phase failure set means any failure at all is a new failure — the gate does not silently tolerate a first failure"
    - "The baseline comparison is over the set of failing test node IDs and is order-independent, so a reordered pytest run cannot change the verdict"
    - "The single known pre-existing flake, tests/market/test_simulator.py::test_custom_update_interval, is re-run in isolation before it is accepted as tolerated, and a reproducible failure is treated as a real regression"
  artifacts:
    - path: "db/finally.db"
      provides: "A freshly seeded, tracked SQLite database carrying the standard first-launch state"
  key_links:
    - from: "db/finally.db"
      to: "backend/app/db/seed.py"
      via: "the committed file is generated by calling apply_schema and seed_fresh, so it matches what lazy init would produce"
      pattern: "seed_fresh"
  prohibitions:
    - requirement_id: CORE-04
      category: safety
      status: resolved
      verification: test
      statement: "MUST NOT destroy a developer's live local database without confirmation. db/finally.db is tracked but is also the running application's working file, so the rewrite must refuse to proceed when the working copy differs from HEAD, rather than silently discarding an uncommitted local session."
    - requirement_id: SETUP-06
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT dismiss a genuine regression as the known flake. The tolerated failure is one named test with a measured, reproducible-on-retry signature; treating any red market-data test as 'the known flake' would convert a diagnostic allowance into a blanket excuse and hide a real break next to the frozen module."
---

<objective>
Replace the tracked `db/finally.db` with a freshly seeded database, and close the phase with the dependency-change regression gate.

Purpose: the tracked database currently ships a used session — roughly $6,200 in cash, twelve watchlist tickers, four positions, forty-six trades and twenty-six chat messages from someone's prior run. Because the lazy-init seed gate correctly asks whether `users_profile` already holds a row, a fresh clone finds one and seeds nothing, so the first launch shows that stranger's portfolio instead of the $10,000 and ten tickers the product specification promises. The phase's own success criterion about a machine with no database file is therefore unobservable on a real clone, and Phase 7's fresh-start E2E test would fail for this reason alone. The user's decision at the research gate was to rewrite the file's contents while keeping it tracked — the constraint recorded in PROJECT.md forbids untracking it, not rewriting it.

Output: a freshly seeded `db/finally.db` still tracked in git, verified by inspection, and a green full-suite run measured against the pre-phase baseline.
</objective>

<execution_context>
@C:/Users/ehasi/OneDrive/Documents/AI Coder Course/Project/finally/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ehasi/OneDrive/Documents/AI Coder Course/Project/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/01-foundation-spine/01-CONTEXT.md
@.planning/phases/01-foundation-spine/01-RESEARCH.md
@.planning/phases/01-foundation-spine/01-VALIDATION.md
@.planning/phases/01-foundation-spine/01-02-SUMMARY.md
@planning/PLAN.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the one-way rewrite of the tracked database</name>
  <decision>Replace the contents of the tracked `db/finally.db` with a freshly seeded database ($10,000 cash, the ten default watchlist tickers, one portfolio snapshot, no positions, trades or chat messages).</decision>
  <context>
    This is the phase's one genuinely irreversible step. `db/finally.db` is a tracked binary, so once the rewrite is committed the previous session survives only as a blob in git history, and every clone, pull and branch checkout touching this path inherits the new contents. There is no in-place edit that restores the old state for anyone who has already pulled.

    Why it is being proposed: the tracked file currently ships a used session — roughly $6,200.01 cash, twelve watchlist tickers including `SPY` and `SRP`, four positions, forty-six trades and twenty-six chat messages. The lazy-init seed gate correctly asks whether `users_profile` already holds a row, finds one, and seeds nothing, so a fresh clone launches into that prior portfolio rather than the $10,000 and ten tickers the product specification promises. Phase 7's fresh-start E2E test would fail for this reason alone.

    What is not being proposed: untracking the file, relocating the repository, or changing the `db/` bind-mount source. All three are out of scope for every plan in this phase — PROJECT.md records the tracked database as a reviewed, accepted risk, and CONTEXT.md forbids untracking it. This changes contents only.

    Decision D-23 already records the user selecting this option at the research gate. This checkpoint exists because the step is destructive to any live local session in that file, not to re-open the product question.
  </context>
  <options>
    <option id="rewrite">
      <name>Rewrite the tracked database with the standard fresh seed (D-23, recommended)</name>
      <pros>A fresh clone shows the promised first-launch state; the committed artifact matches what lazy init produces; unblocks Phase 7's fresh-start E2E test; every accepted risk stays intact</pros>
      <cons>Irreversible for anyone who has already pulled; discards the prior session's simulated portfolio</cons>
    </option>
    <option id="keep">
      <name>Keep the current contents and restate the success criterion</name>
      <pros>Nothing is destroyed; the phase's database criteria remain testable against `tmp_path` databases, which the test fixtures already use</pros>
      <cons>First launch keeps showing $6,200.01 and twelve tickers; Phase 7's fresh-start E2E test stays blocked; the committed artifact and the seed path stay out of agreement</cons>
    </option>
  </options>
  <resume-signal>Select: rewrite or keep</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Rewrite the tracked database with the standard fresh seed</name>
  <precondition>The working copy of `db/finally.db` matches HEAD — `git status --porcelain db/finally.db` returns nothing. If it reports a modification, the developer has an uncommitted live session in that file and this task must halt rather than discard it; surface the difference and ask before proceeding.</precondition>
  <reversibility rating="one-way">The file is a tracked binary. Once the rewrite is committed, the previous session's contents survive only in git history as a binary blob, and every clone and every branch checkout that touches this path inherits the new contents. There is no in-place edit that restores the old state for a user who has already pulled.</reversibility>
  <files>db/finally.db</files>
  <read_first>
    - backend/app/db/seed.py — `apply_schema`, `is_fresh_database`, `seed_fresh`, `DEFAULT_TICKERS` and `STARTING_CASH`, which are what generate the committed artifact
    - backend/app/db/connection.py — `connect` and `ensure_initialized`, the path the running application takes, so the committed file matches what lazy init would produce
    - backend/app/db/schema.sql — the six-table DDL
    - .planning/phases/01-foundation-spine/01-CONTEXT.md — decision D-23 and the `<specifics>` note fixing what is out of scope
    - .planning/PROJECT.md — the Key Decisions table entry recording the tracked database as a reviewed, accepted risk
    - .planning/phases/01-foundation-spine/01-RESEARCH.md — Pitfall 3, which enumerates the exact contents currently in the tracked file
  </read_first>
  <action>
Replace the contents of the tracked `db/finally.db` with a freshly seeded database, implementing D-23.

Generate it by calling this phase's own code rather than by writing SQL by hand. Delete the existing file, then run a short one-off invocation of `ensure_initialized` against the repo-root `db/finally.db` path — or equivalently `apply_schema` followed by `seed_fresh` inside a `writing()` transaction — so the committed artifact is byte-for-byte the product of the same path a first request would take. Generating it any other way creates a second definition of what a seeded database is, which is exactly the divergence that produced the current problem.

Run it through `uv run` from `backend/`, per the project's package-manager rule; never a bare Python invocation.

Afterwards the file must contain: the six tables; exactly one `users_profile` row with `cash_balance` 10000.0; exactly ten `watchlist` rows whose ticker set equals `DEFAULT_TICKERS`; exactly one `portfolio_snapshots` row with `total_value` 10000.0; and zero rows in `positions`, `trades` and `chat_messages`. The twelve tickers presently in the file include `SPY` and `SRP`, which are not in the default set — their absence afterwards is the quickest signal the rewrite took effect.

Then checkpoint the WAL and remove the sidecars before committing. The generation step runs with `journal_mode=WAL`, so it leaves `db/finally.db-wal` and `db/finally.db-shm` beside the database; run a WAL checkpoint so all content is folded into the main file, then delete both sidecars. Plan 01-04 added ignore rules for them, so they will not be committed either way, but a database committed with pending WAL content is a database whose committed bytes do not carry the seed.

Keep the file tracked. Do not run any command that removes it from the index, and do not add an ignore rule matching it. PROJECT.md records keeping it tracked as a reviewed user decision, and CONTEXT.md's process constraints forbid untracking it, relocating the repository, or changing the `db/` bind-mount source. This task changes contents only.

Verify the result by querying the committed file directly and confirm `git ls-files db/finally.db` still reports it, then commit the single file.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -c "import sqlite3,pathlib; p=pathlib.Path('..')/'db'/'finally.db'; c=sqlite3.connect(p); print(c.execute('SELECT cash_balance FROM users_profile').fetchall(), c.execute('SELECT COUNT(*) FROM watchlist').fetchone(), c.execute('SELECT COUNT(*) FROM positions').fetchone(), c.execute('SELECT COUNT(*) FROM trades').fetchone(), c.execute('SELECT COUNT(*) FROM chat_messages').fetchone(), c.execute('SELECT COUNT(*) FROM portfolio_snapshots').fetchone())"</automated>
    <automated>git ls-files db/finally.db</automated>
  </verify>
  <acceptance_criteria>
    - Querying `db/finally.db` returns exactly one `users_profile` row and its `cash_balance` equals 10000.0
    - `SELECT COUNT(*) FROM watchlist` on `db/finally.db` returns 10, and the ticker set equals `DEFAULT_TICKERS`
    - `SELECT COUNT(*) FROM portfolio_snapshots` returns 1 and its `total_value` equals 10000.0
    - `SELECT COUNT(*) FROM positions`, `SELECT COUNT(*) FROM trades` and `SELECT COUNT(*) FROM chat_messages` each return 0
    - The ticker set contains neither `SPY` nor `SRP`
    - `SELECT name FROM sqlite_master WHERE type='table'` returns the six expected table names
    - `git ls-files db/finally.db` outputs `db/finally.db`, confirming the file is still tracked
    - `git check-ignore -q db/finally.db` exits non-zero, confirming no ignore rule was added for it
    - No `db/finally.db-wal` or `db/finally.db-shm` file remains on disk after the commit
    - `git status --porcelain` is clean after the commit
  </acceptance_criteria>
  <done>The tracked database carries the standard fresh seed, is still tracked, and was generated by this phase's own seed path rather than by hand.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the committed database contents</name>
  <what-built>The tracked `db/finally.db` has been replaced with a freshly seeded database: six tables, one profile at $10,000, the ten default watchlist tickers, one portfolio snapshot, and no positions, trades or chat messages. The file is still tracked in git. This is the one-time content check that Phase 1's validation strategy records as manual-only — asserting on a committed binary is not a repeatable unit test.</what-built>
  <how-to-verify>
    1. Confirm the file is still tracked: run `git ls-files db/finally.db` at the repository root. It should print `db/finally.db`.
    2. Inspect the contents. From `backend/`, run `uv run python -c "import sqlite3; c=sqlite3.connect('../db/finally.db'); print('cash', c.execute('SELECT cash_balance FROM users_profile').fetchall()); print('watchlist', sorted(r[0] for r in c.execute('SELECT ticker FROM watchlist'))); print('snapshots', c.execute('SELECT COUNT(*) FROM portfolio_snapshots').fetchone()); print('positions', c.execute('SELECT COUNT(*) FROM positions').fetchone()); print('trades', c.execute('SELECT COUNT(*) FROM trades').fetchone()); print('chat', c.execute('SELECT COUNT(*) FROM chat_messages').fetchone())"`
    3. Expected: cash `[(10000.0,)]`; watchlist exactly `['AAPL', 'AMZN', 'GOOGL', 'JPM', 'META', 'MSFT', 'NFLX', 'NVDA', 'TSLA', 'V']`; snapshots `(1,)`; positions `(0,)`; trades `(0,)`; chat `(0,)`.
    4. Confirm what is gone: the watchlist should no longer contain `SPY` or `SRP`, and the cash figure should no longer be $6,200.01.
    5. Optionally start the app and open `http://localhost:8000/api/health` to confirm the backend still answers against this database.

    This is deliberate and irreversible for anyone who pulls the commit. If you were keeping the prior session's portfolio for any reason, say so now rather than approving.
  </how-to-verify>
  <resume-signal>Type "approved" if the database shows $10,000 and the ten default tickers, or describe what you see instead</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Dependency-change regression gate against the pre-phase baseline</name>
  <files>backend/tests/market/test_simulator.py</files>
  <read_first>
    - .planning/phases/01-foundation-spine/01-VALIDATION.md — the D-24 gate wording, the sampling contract and the full-suite command
    - .planning/phases/01-foundation-spine/01-RESEARCH.md — Pitfall 1, including the measured flake rate and the reason the test is marginal on Windows timer granularity
    - .planning/phases/01-foundation-spine/01-CONTEXT.md — decision D-24 and the instruction not to modify anything under `tests/market/` to chase the flake
    - backend/tests/market/test_simulator.py — the `test_custom_update_interval` test itself, read only to confirm its identity and timing assertion; it is not edited
  </read_first>
  <action>
Close the phase by proving that the dependency changes introduced no regression in the frozen market data module's existing tests (SETUP-06).

The gate is *no new failures relative to the pre-phase baseline*, not a literal 154 of 154. That restatement is decision D-24 and it exists because one test, `tests/market/test_simulator.py::test_custom_update_interval`, fails roughly three runs in ten on the unmodified pre-phase environment: it starts a simulator at a 0.01 second interval, sleeps 0.05 seconds, and asserts at least three ticks occurred, which is marginal against Windows' roughly 15.6 millisecond default timer granularity and slips under any load. The FastAPI bump did not cause it and no work in this phase can fix it.

Run the full suite from `backend/` with `uv run --extra dev pytest -q` and capture the set of failing test node IDs. Compare that set against the pre-phase baseline. Treat the comparison as a set comparison over node IDs, not a count and not an ordering: a run whose failing set is exactly equal to the baseline passes; a run with even one additional node ID fails the gate. Note that the baseline for everything except the one known flake is empty, which means any other failure at all is a new failure — the gate has no allowance for a first failure.

If `test_custom_update_interval` is the only red test, re-run it in isolation a few times before accepting it as the tolerated flake. A test that fails every single time in isolation is reproducible and is therefore a real regression, not the flake, and must be diagnosed to root cause rather than waved through. This distinction is the whole point of the allowance: it covers one named test with a measured intermittent signature, and it is not a blanket excuse for any red result under `tests/market/`.

Do not modify anything under `backend/tests/market/` to make the gate pass. That directory is adjacent to the frozen module and CONTEXT.md is explicit that the flake is recorded as tolerated rather than chased. The file appears in this task's file list only because it is read and re-run, not because it is edited — leave it byte-identical.

Also run the lint gate, `uv run --extra dev ruff check app/ tests/`, and confirm a clean frozen-sync import of the chat dependencies, which is the fourth of the phase's success criteria and is what proves the committed lockfile is what Phase 2's Docker build will rely on.

Record the outcome in the plan summary: the failing set, whether it equalled the baseline, and how many isolated re-runs the flake needed.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run --extra dev pytest -q</automated>
    <automated>cd backend &amp;&amp; uv run --extra dev ruff check app/ tests/</automated>
    <automated>cd backend &amp;&amp; uv run --frozen python -c "import litellm, pydantic, dotenv, httpx"</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run --extra dev pytest -q` completes and its set of failing test node IDs is either empty or exactly `{tests/market/test_simulator.py::test_custom_update_interval}`
    - Every test under `backend/tests/market/` other than `test_custom_update_interval` passes
    - If `test_custom_update_interval` failed, `cd backend && uv run --extra dev pytest -q "tests/market/test_simulator.py::test_custom_update_interval"` passes on at least one of three isolated re-runs, confirming the intermittent signature
    - `git diff --stat HEAD -- backend/tests/market/test_simulator.py` is empty, confirming the file was not edited
    - `cd backend && uv run --extra dev ruff check app/ tests/` exits 0
    - `cd backend && uv run --frozen python -c "import litellm, pydantic, dotenv, httpx"` exits 0
    - `git status --porcelain backend/app/market/` returns nothing, confirming the frozen module is untouched
  </acceptance_criteria>
  <done>The full suite shows no new failures relative to the pre-phase baseline, the lint gate is clean, and a frozen sync from the committed lockfile imports the chat dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generation script -> tracked `db/finally.db` | A destructive write crosses into a committed, shared artifact |
| local working tree -> git history | The rewritten binary reaches every clone permanently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-15 | Tampering | destructive rewrite of a tracked database | high | mitigate | The task carries a precondition requiring `git status --porcelain db/finally.db` to be empty, so an uncommitted live session halts the rewrite; a blocking human-verify checkpoint confirms the resulting contents before the phase closes |
| T-1-16 | Information Disclosure | the prior session's data persisting in git history | low | accept | The replaced contents are simulated-portfolio data for a single local operator with no real money, no credentials and no personal data. Accepted at ASVS L1 |
| T-1-17 | Repudiation | a real regression waved through as the known flake | medium | mitigate | The gate names one specific test node ID, requires isolated re-runs before accepting it, and treats a reproducible failure as a regression; every other market-data test must pass outright |
| T-1-06 | Tampering | WAL sidecars left beside the generated database | medium | mitigate | The generation step checkpoints the WAL and deletes both sidecars before committing, and plan 01-04's ignore rules mean neither could be committed regardless |
</threat_model>

<assumption_delta_decision>
**Signal:** `pluralization` on the term "fallback", from "routes take precedence over the static frontend fallback."

**Noun now primary:** none.

**Decision:** `no-change`

**Rationale:** The term names a single Starlette behavior for unmatched routes, not an abstraction that gained a second implementation; the codebase's one real singular-to-plural is the pair of market data sources, already abstracted behind the frozen `MarketDataSource` ABC and untouched by this phase.
</assumption_delta_decision>

<flagged_assumptions>
Probe rows carried by this plan. The three `SETUP-06` rows resolved to explicit acceptance criteria rather than assumptions and appear in `must_haves.truths`; no `unclassified` row falls to this plan.

The one assumption worth recording explicitly, from RESEARCH.md's assumptions log rather than the edge probe: `backend/tests/market/` is treated as **not** covered by the `backend/app/market/` freeze for the purpose of reading and re-running its tests. This plan reads and re-runs that directory but edits nothing in it, so the distinction does not become load-bearing here. If a future phase wants to fix the timing tolerance in `test_custom_update_interval`, that is a user decision and not an executor's.
</flagged_assumptions>

<artifacts_this_phase_produces>
This plan creates no new source symbols. It rewrites the contents of one existing tracked artifact and runs the phase's closing gates.

**Rewritten artifact:** `db/finally.db` — replaced in place with a freshly seeded database generated by `apply_schema` and `seed_fresh` from `backend/app/db/seed.py` (both created earlier in this phase by plan 01-02)

**Read-only artifacts referenced:** `backend/tests/market/test_simulator.py::test_custom_update_interval`, the one named tolerated flake
</artifacts_this_phase_produces>

<verification>
- `db/finally.db` queries clean: $10,000 cash, ten default tickers, one snapshot, no positions, no trades, no chat messages
- `git ls-files db/finally.db` still reports the file as tracked
- `cd backend && uv run --extra dev pytest -q` has a failing set equal to the pre-phase baseline
- `cd backend && uv run --extra dev ruff check app/ tests/` exits 0
- `cd backend && uv run --frozen python -c "import litellm, pydantic, dotenv, httpx"` exits 0
- `git status --porcelain backend/app/market/` returns nothing
- Nothing in this plan relocates the repository, changes the `db/` bind-mount source, or untracks `db/finally.db`
</verification>

<success_criteria>
- A fresh clone launches into the product's promised first-launch state: $10,000 cash and the ten default watchlist tickers
- The tracked database is regenerated by the same seed path the running application uses, so the committed artifact and lazy init cannot disagree
- `uv sync --frozen` from the committed lockfile produces an environment where the chat dependencies import and the existing market-data tests show no new failures
</success_criteria>

<output>
Create `.planning/phases/01-foundation-spine/01-05-SUMMARY.md` when done
</output>
