---
phase: 01-foundation-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/.python-version
  - backend/app/config.py
  - backend/app/api/__init__.py
  - backend/app/api/health.py
  - backend/app/main.py
  - backend/static/index.html
  - backend/tests/conftest.py
  - backend/tests/test_main.py
  - backend/tests/api/__init__.py
  - backend/tests/api/test_health.py
  - backend/tests/market/test_stream_integration.py
autonomous: true
requirements: [SETUP-01, SETUP-02, CORE-01, CORE-02, CORE-03, CORE-07, CORE-08, CORE-09]

estimate:
  tokens: 60000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GET /api/health on the running app returns 200 with exactly the keys status, market_source, tickers_cached, newest_price_age_seconds"
    - "GET /api/stream/prices on the running app returns 200 text/event-stream, emits retry: 1000 as its first frame, and carries at least one data: frame naming a tracked ticker"
    - "The SSE generator emits a heartbeat comment frame beginning with ': ping' when driven with a short heartbeat interval"
    - "GET / returns 200 text/html serving backend/static/index.html"
    - "GET /api/health is not shadowed by the static frontend: it returns the health JSON, not index.html"
    - "GET /api/nope with Accept: application/json returns 404 with a JSON body; GET /api/nope with Accept: text/html returns 200 text/html (the Accept-gated SPA fallback)"
    - "create_app() constructs PriceCache and the market data source in the factory body before any include_router() call, so the router factories receive a live cache"
    - "The market data source is started by the lifespan enter and stopped by the lifespan exit; no deprecated @app.on_event handler exists"
    - "app.state.price_cache holds the same PriceCache object the routers were built with, and no module-level PriceCache singleton exists in app/main.py"
    - "newest_price_age_seconds is None before any price has been written to the cache, and a non-negative float once a price exists"
    - "Two concurrent SSE clients each receive their own frames from the same running app, and one client disconnecting does not terminate the other's stream"
    - "GET /api/health returns 200 with its four-key payload while an SSE stream is held open on the same running app, so a long-lived stream does not stall a concurrent health check"
    - "The Accept-gated route precedence holds under a live stream: with an SSE client connected, GET /api/nope with Accept: application/json still returns a 404 JSON body and never index.html"
    - "uv sync --frozen --extra dev from the committed lockfile yields an environment where litellm, pydantic, dotenv and httpx all import successfully"
    - "An interrupted uv sync leaves a partially installed venv that a plain re-run does not repair; deleting backend/.venv and re-syncing with UV_LINK_MODE=copy restores a complete, importable environment"
    - statement: "Concurrent requests to / and to /api/* are served independently under the app.frontend() registration; neither blocks nor corrupts the other"
      verification: backstop
  artifacts:
    - path: "backend/app/main.py"
      provides: "create_app() factory, lifespan handler, absolute STATIC_DIR"
      contains: "def create_app"
    - path: "backend/app/config.py"
      provides: "load_dotenv on the project-root .env plus DB_PATH, OPENROUTER_API_KEY, MASSIVE_API_KEY, LLM_MOCK constants"
      contains: "FINALLY_DB_PATH"
    - path: "backend/app/api/health.py"
      provides: "create_health_router(cache, source) router factory for GET /api/health"
      contains: "def create_health_router"
    - path: "backend/static/index.html"
      provides: "Committed placeholder so the static fallback directory always exists"
    - path: "backend/pyproject.toml"
      provides: "fastapi>=0.141.1 floor plus litellm, pydantic, python-dotenv runtime deps and httpx dev extra"
      contains: "fastapi>=0.141.1"
    - path: "backend/tests/test_main.py"
      provides: "End-to-end spine test plus lifespan, DI and route-precedence assertions"
    - path: "backend/tests/market/test_stream_integration.py"
      provides: "Uvicorn-in-thread SSE integration test and the heartbeat assertion"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/market/stream.py"
      via: "include_router(create_stream_router(cache)) with the cache built earlier in the factory body"
      pattern: "create_stream_router\\(cache\\)"
    - from: "backend/app/main.py"
      to: "backend/app/api/health.py"
      via: "include_router(create_health_router(...)) with the same cache instance"
      pattern: "create_health_router\\("
    - from: "backend/app/main.py"
      to: "backend/static/index.html"
      via: "app.frontend('/', directory=STATIC_DIR, fallback='index.html') with STATIC_DIR absolute"
      pattern: "app\\.frontend\\("
    - from: "backend/app/main.py"
      to: "backend/app/config.py"
      via: "app.state.db_path = DB_PATH so Phase 3 routers resolve the database through DI"
      pattern: "app\\.state\\.db_path"
  prohibitions:
    - requirement_id: CORE-08
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT report a healthy status while the price feed is dead. The health payload must carry newest_price_age_seconds truthfully so an operator can tell a live stream from a stalled one, rather than presenting a hardcoded green signal that hides a stopped market source."
---

<objective>
Assemble the FastAPI application for the first time and prove the whole spine end to end: the app builds, the market data source runs under lifespan, `GET /api/health` answers, `GET /api/stream/prices` streams live prices with heartbeats, and the static frontend is served from the same origin without shadowing any `/api/*` route.

Purpose: `backend/app/market/` has been finished and frozen for a while but nothing consumes it — there is no `main.py`, so the SSE router has never been mounted and has never been exercised over HTTP. This plan creates the object graph everything else in the project hangs from, and it is deliberately the first thing built so an architectural mistake in the assembly surfaces after one commit rather than after the database layer is stacked on top of it.

Output: `backend/app/config.py`, `backend/app/api/health.py`, `backend/app/main.py`, `backend/static/index.html`, a raised dependency floor with a regenerated lockfile, and a test suite that drives the assembled app over real HTTP.
</objective>

<execution_context>
@C:/Users/ehasi/OneDrive/Documents/AI Coder Course/Project/finally/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ehasi/OneDrive/Documents/AI Coder Course/Project/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-spine/01-CONTEXT.md
@.planning/phases/01-foundation-spine/01-RESEARCH.md
@.planning/phases/01-foundation-spine/01-PATTERNS.md
@planning/PLAN.md
@backend/CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end spine — dependency floor, create_app(), health, stream, static, one path proven over HTTP</name>
  <precondition>`uv` is on PATH and `UV_LINK_MODE=copy` is exported for this shell. This repository lives on a OneDrive-backed tree where `uv sync` otherwise fails with `failed to hardlink file ... (os error 396)` and leaves a half-installed venv that a plain re-run does not repair. If a sync has already failed this way, delete `backend/.venv` before re-syncing.</precondition>
  <reversibility rating="costly">Phase 2's Dockerfile and start scripts, and every Phase 3 and Phase 6 router, are written against the `create_app()` seam, the `FINALLY_DB_PATH` variable name and the `app.state` attribute names fixed here; changing them later is a coordinated edit across four phases.</reversibility>
  <files>backend/pyproject.toml, backend/uv.lock, backend/.python-version, backend/app/config.py, backend/app/api/__init__.py, backend/app/api/health.py, backend/app/main.py, backend/static/index.html, backend/tests/test_main.py</files>
  <read_first>
    - backend/pyproject.toml — the current dependency block, ruff config and pytest config being modified
    - backend/app/market/__init__.py — the eight exported names that are the entire legal surface across the freeze
    - backend/app/market/stream.py — `create_stream_router(price_cache)`, the router-factory convention `create_health_router` must mirror, and the `_generate_events` keyword arguments
    - backend/app/market/factory.py — `create_market_data_source(price_cache)`, the env-read idiom, and the "log the presence of a key, never its value" rule
    - backend/app/market/cache.py — `PriceCache`, `newest_timestamp()`, `__len__`, `wait_for_price`
    - backend/app/market/interface.py — the `MarketDataSource` lifecycle contract and `source_name`
    - backend/app/market/seed_prices.py — the module-constant style to imitate in `config.py`
    - .planning/phases/01-foundation-spine/01-RESEARCH.md — Code Examples (verified `create_app()` skeleton, `app.frontend()` signature, health payload) and Pitfalls 2, 5, 6, 7
    - .planning/phases/01-foundation-spine/01-PATTERNS.md — the `health.py` / `main.py` / `config.py` pattern assignments
    - planning/PLAN.md sections 5, 8 and 11 — env vars, the `/api/health` payload, the static-serving rule
  </read_first>
  <action>
Wire one request path through every layer this phase touches, end to end, and prove it over real HTTP. Production quality, not a spike — this code stays.

Dependency floor first, because `app.frontend()` does not exist below FastAPI 0.141.1. From `backend/`, run `uv add "fastapi>=0.141.1" "litellm>=1.95.0" "pydantic>=2.10.0" "python-dotenv>=1.0.0"` then `uv add --optional dev "httpx>=0.28.0"` (per D-25 and SETUP-01/SETUP-02), then `uv sync --frozen --extra dev`. Commit the regenerated `backend/uv.lock`. Also write `backend/.python-version` containing exactly `3.12` (D-28) so the local runtime matches the Python 3.12 the Docker image will target. Do not pin `openai` — RESEARCH.md Pitfall 6b diagnosed the tempting `ModuleNotFoundError` naming `openai.types.responses.*` to a Windows MAX_PATH limit at a deep scratch path, not a version conflict.

`backend/app/config.py` (D-13): call `load_dotenv` on the project-root `.env` at import — resolve it by walking up from `Path(__file__)`, not from the process CWD, because the backend runs from `backend/` and loads `../.env`. Expose `PROJECT_ROOT`, `DB_PATH`, `OPENROUTER_API_KEY`, `MASSIVE_API_KEY` and `LLM_MOCK` as module constants read with plain `os.environ.get(..., "").strip()`. `DB_PATH` resolves from the `FINALLY_DB_PATH` environment variable (D-12), defaulting to the repo-root `db/finally.db` reached by walking up from the package — not `backend/db/`, which is where a naive relative derivation lands and which diverges from the tracked, bind-mounted top-level `db/`. No `pydantic-settings`.

`backend/app/api/__init__.py`: package init following the `app/market/__init__.py` shape — module docstring with a `Public API:` block, relative import of `create_health_router`, sorted `__all__`.

`backend/app/api/health.py` (D-14, CORE-08): a `create_health_router(price_cache, source)` factory returning an `APIRouter(prefix="/api", tags=["system"])` with one `GET /health` handler closing over its arguments. Build the router inside the factory, not at module level, so constructing an app twice (real plus test) does not register the route twice — carry that reasoning into the docstring, as `stream.py` does. The payload is exactly four keys: `status` fixed to `"ok"`, `market_source` from `source.source_name`, `tickers_cached` from `len(price_cache)`, and `newest_price_age_seconds` computed from `price_cache.newest_timestamp()` as `None` when no price exists yet, otherwise `round(time.time() - newest, 3)`. Do not compute any of these by hand — all three inputs already exist on the frozen module for exactly this purpose. Never place a key value in the payload or in a log line.

`backend/app/main.py` (CORE-01, CORE-02, CORE-07, CORE-09): module-level `STATIC_DIR = Path(__file__).resolve().parent.parent / "static"` — absolute, because `app.frontend()`'s `directory` is keyword-only and resolves against the process CWD, and `check_dir="auto"` raises at app-creation time, so a relative path errors during test collection depending on where the process was launched (RESEARCH.md Pitfall 5). Then `create_app() -> FastAPI` whose body, in this order: constructs `cache = PriceCache()`, constructs `source = create_market_data_source(cache)`, defines an `@asynccontextmanager` `lifespan` that awaits `source.start(DEFAULT_TICKERS)` before `yield` and `await source.stop()` after, builds `FastAPI(title="FinAlly", lifespan=lifespan)`, sets `app.state.price_cache`, `app.state.market_source` and `app.state.db_path = DB_PATH`, calls `include_router(create_health_router(cache, source))` then `include_router(create_stream_router(cache))`, and finally `app.frontend("/", directory=STATIC_DIR, fallback="index.html")`. The cache-before-router ordering is load-bearing and is the single most likely way to get this file wrong: both router factories take the cache as an argument and `include_router()` runs long before lifespan does, so constructing the cache inside the lifespan handler cannot work. Do not add an `if` guard around `source.stop()` — `stop()` is documented idempotent and a guard would be defensive programming. Do not use `@app.on_event`. The ten default tickers this phase starts the source with are AAPL, GOOGL, MSFT, AMZN, TSLA, NVDA, META, JPM, V and NFLX; name them locally in `main.py` for now rather than importing `SEED_PRICES`, and Task 1 of plan 01-02 will move the canonical list into `app/db/seed.py` as `DEFAULT_TICKERS`.

`backend/static/index.html` (D-11): a small committed placeholder page stating that the FinAlly backend is running and pointing at `/api/health`. Dark background consistent with the product palette. No emojis anywhere in the markup. This file is what makes the static fallback real from day one and what Phase 7's Next.js export will replace.

`backend/tests/test_main.py`: one end-to-end test, `test_spine_end_to_end`, using the uvicorn-in-thread pattern — a `uvicorn.Server` on an ephemeral port in a daemon thread, polled on `server.started`, torn down with `should_exit` and a joined thread. This is not a style preference: both `TestClient.stream()` and `httpx.ASGITransport` hang forever against the infinite SSE generator on this project's pinned starlette and httpx, never returning headers, so no read timeout rescues them (RESEARCH.md Pitfall 2). Build the app with `create_app()` so lifespan actually runs and the simulator populates the cache. In one test assert: `/api/health` returns 200 with the four keys; `/api/stream/prices` returns 200 `text/event-stream` whose first frame is `retry: 1000` and which carries a `data:` frame; `/` returns 200 `text/html`. Set `MASSIVE_API_KEY` to empty for the test so a developer holding a real key does not have the suite poll the live Massive API.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run --extra dev pytest -q tests/test_main.py::test_spine_end_to_end -x</automated>
    <automated>cd backend &amp;&amp; uv run --frozen python -c "import litellm, pydantic, dotenv, httpx, fastapi; print(fastapi.__version__)"</automated>
    <automated>cd backend &amp;&amp; uv run --extra dev ruff check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` contains `fastapi>=0.141.1`, `litellm>=1.95.0`, `pydantic>=2.10.0` and `python-dotenv>=1.0.0` in `[project].dependencies`, and `httpx>=0.28.0` in `[project.optional-dependencies].dev`
    - `backend/uv.lock` is regenerated and committed; `cd backend && uv sync --frozen --extra dev` completes with no resolution error
    - `cd backend && uv run --frozen python -c "import litellm, pydantic, dotenv, httpx"` exits 0
    - `backend/.python-version` contains exactly `3.12`
    - `backend/app/config.py` defines `PROJECT_ROOT`, `DB_PATH`, `OPENROUTER_API_KEY`, `MASSIVE_API_KEY`, `LLM_MOCK` and reads the variable name `FINALLY_DB_PATH`
    - With `FINALLY_DB_PATH` unset, `config.DB_PATH` resolves to the repo-root `db/finally.db` (the sibling of `backend/`), not to `backend/db/finally.db`
    - `backend/app/main.py` defines `create_app` and `STATIC_DIR`; `STATIC_DIR` is built from `Path(__file__).resolve()` and is absolute
    - In `create_app()`, the `PriceCache()` construction and the `create_market_data_source(...)` call both appear before the first `include_router(` call
    - `backend/app/main.py` contains no `on_event` handler
    - `backend/app/api/health.py` defines `create_health_router` and returns an `APIRouter` built inside the factory body
    - `cd backend && uv run --extra dev pytest -q tests/test_main.py::test_spine_end_to_end -x` exits 0
    - `GET /api/health` on the running app returns 200 and a JSON object whose key set is exactly `{status, market_source, tickers_cached, newest_price_age_seconds}`
    - `GET /api/stream/prices` on the running app returns 200 with a `content-type` starting `text/event-stream`, first frame `retry: 1000`, and at least one frame starting `data: `
    - `GET /` returns 200 with a `content-type` starting `text/html`
    - `backend/static/index.html` exists and contains the string `/api/health`
    - `cd backend && uv run --extra dev ruff check app/ tests/` exits 0
  </acceptance_criteria>
  <done>The assembled app starts, serves health, streams prices and serves the static placeholder, all proven by one automated end-to-end test against a real HTTP server, and the dependency floor supports it from the committed lockfile.</done>
</task>

<task type="auto">
  <name>Task 2: Expand spine coverage — lifespan, DI, route precedence, SSE integration and heartbeat, shared app fixture</name>
  <files>backend/tests/conftest.py, backend/tests/test_main.py, backend/tests/api/__init__.py, backend/tests/api/test_health.py, backend/tests/market/test_stream_integration.py</files>
  <read_first>
    - backend/tests/conftest.py — the existing four-line fixture file to extend in place, not replace
    - backend/tests/market/test_cache.py — the house test-module shape: `Test<Subject>` class, one docstring line per test, absolute `app.market.<module>` imports
    - backend/app/main.py — the `create_app()` written in Task 1, its `app.state` attribute names and lifespan shape
    - backend/app/api/health.py — the health payload written in Task 1
    - backend/app/market/stream.py — `_generate_events(cache, request, interval=, heartbeat=)`, the keyword arguments the heartbeat test drives directly
    - .planning/phases/01-foundation-spine/01-RESEARCH.md — the verified uvicorn-in-thread fixture, the heartbeat probe, and Pitfall 7's Accept-header result table
    - .planning/phases/01-foundation-spine/01-VALIDATION.md — the per-requirement test map and the Wave 0 file list
  </read_first>
  <action>
Build out the spine's test coverage from the proven slice. Every new test file gets `-> None` return annotations and annotated fixtures — the stricter project rule wins for new files (PATTERNS.md records the conflict explicitly). Do not retrofit or otherwise touch anything under `backend/tests/market/` except the one new file named below; the frozen module's existing 154 tests stay exactly as they are.

Extend `backend/tests/conftest.py` in place, keeping the existing `event_loop_policy` fixture. Add (D-22) a session-scoped autouse fixture that clears `MASSIVE_API_KEY` from the environment for the whole test session — `create_market_data_source()` reads it at call time, so a developer with a real key would otherwise have the suite poll the live Massive API. Add an `app` fixture built around `create_app()` and a `db_path` fixture backed by pytest's `tmp_path`, wired together by overriding the FastAPI database-path dependency on the constructed app rather than by monkeypatching `FINALLY_DB_PATH`, which would depend on import-time ordering in `config.py`. The dependency provider itself lands in plan 01-02's `app/db/connection.py` as `get_db_path`; until it exists, set `app.state.db_path` to the `tmp_path` file directly and leave a one-line docstring note that plan 01-02 replaces this with `dependency_overrides[get_db_path]`. Phase 3's route tests reuse this fixture verbatim, so name it `app` and keep it importable without arguments beyond `tmp_path`.

`backend/tests/api/__init__.py` and (if absent) `backend/tests/db/__init__.py` are not needed here beyond `tests/api/` — create `backend/tests/api/__init__.py` only.

`backend/tests/api/test_health.py` (CORE-08): assert the exact four-key payload against a `create_app()` app; assert `status` is `"ok"`; assert `market_source` is `"simulator"` when `MASSIVE_API_KEY` is empty; assert `tickers_cached` is an int; assert `newest_price_age_seconds` is `None` on a cache with no prices and a non-negative float once `cache.update("AAPL", 190.50)` has run. Assert the payload contains no key whose name mentions an API key — the health endpoint must stay a four-key surface.

Extend `backend/tests/test_main.py` with the remaining assembly tests:
`test_create_app_builds_cache_before_routers` — `create_app()` returns an app whose `state.price_cache` is a `PriceCache` and whose route paths include both `/api/health` and `/api/stream/prices` (CORE-01).
`test_lifespan_starts_and_stops_source` — drive the app through its lifespan and assert the source moved from unstarted to started and back to stopped; assert `state.market_source` is the object the lifespan drove (CORE-02).
`test_cache_via_dependency` — the object on `app.state.price_cache` is the same instance the routers were built with, and two separate `create_app()` calls produce two distinct caches, proving there is no module-level singleton (CORE-07).
`test_api_not_shadowed` — over the uvicorn-in-thread server, assert the four rows of the verified Accept matrix: `/api/health` returns the health JSON and not `index.html`; `/api/nope` with `Accept: application/json` returns 404 JSON; `/api/nope` with `Accept: */*` returns 404 JSON; `/api/nope` with `Accept: text/html` returns 200 `text/html`. The last row is correct behavior, not a defect — the SPA fallback is Accept-gated, and `fetch()` sends `*/*`, so the frontend's own calls always receive JSON 404s (CORE-09, RESEARCH.md Pitfall 7).
`test_concurrent_health_and_stream` — with an SSE stream held open against the same running server, assert both concurrency edges in the one test: a `/api/health` request still returns 200 promptly with its four-key payload (CORE-08 concurrency edge), and a `/api/nope` request carrying `Accept: application/json` still returns a 404 JSON body rather than the static fallback (CORE-09 concurrency edge). Route precedence is a registration-order property and a held-open stream occupies a connection without changing the routing table, so both must hold while the stream is live — asserting them only against an idle server would leave the phase's one genuinely concurrent path unmeasured.

`backend/tests/market/test_stream_integration.py` (D-21, CORE-03): the uvicorn-in-thread fixture from RESEARCH.md, seeding the cache directly with `cache.update("AAPL", 190.50)` so no simulator is needed. Assert the stream opens 200 `text/event-stream`, the first frame is `retry: 1000`, and a `data:` frame naming AAPL arrives. Add `test_two_clients_stream_independently` — open two concurrent `httpx.stream` connections, confirm both receive a `data:` frame, close one, and confirm the other still receives a further frame. Assert the heartbeat in a separate test that drives `_generate_events(cache, FakeRequest(), interval=0.01, heartbeat=0.05)` directly and looks for a frame starting `: ping`, then closes the generator — those keyword arguments exist for exactly this, and waiting fifteen real seconds does not belong in a unit suite.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run --extra dev pytest -q tests/test_main.py tests/api tests/market/test_stream_integration.py</automated>
    <automated>cd backend &amp;&amp; uv run --extra dev ruff check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run --extra dev pytest -q tests/test_main.py tests/api tests/market/test_stream_integration.py` exits 0 and reports at least 8 passing tests
    - `backend/tests/conftest.py` still defines `event_loop_policy` and additionally defines an `app` fixture and a session-scoped fixture that removes `MASSIVE_API_KEY` from `os.environ`
    - `backend/tests/api/__init__.py` exists
    - `backend/tests/test_main.py` defines `test_create_app_builds_cache_before_routers`, `test_lifespan_starts_and_stops_source`, `test_cache_via_dependency`, `test_api_not_shadowed` and `test_concurrent_health_and_stream`
    - `backend/tests/market/test_stream_integration.py` defines a heartbeat test asserting a frame starting `: ping`, and `test_two_clients_stream_independently`
    - `backend/tests/market/test_stream_integration.py` starts a real `uvicorn.Server` in a thread and does not import `starlette.testclient` or `httpx.ASGITransport`
    - `test_api_not_shadowed` asserts all four Accept rows: `application/json` -> 404 JSON, `*/*` -> 404 JSON, `text/html` -> 200 text/html, and `/api/health` -> health JSON
    - `test_concurrent_health_and_stream` holds an SSE stream open and, while it is open, asserts both a 200 four-key `/api/health` response and a 404 JSON `/api/nope` response under `Accept: application/json`
    - Every test function in the five files listed in `<files>` carries a `-> None` return annotation
    - `git status --porcelain backend/tests/market/` lists only `test_stream_integration.py`
    - `cd backend && uv run --extra dev ruff check app/ tests/` exits 0
  </acceptance_criteria>
  <done>The assembled app's lifespan, dependency injection, route precedence, SSE streaming and heartbeat are each covered by an automated test, and a reusable `create_app()` fixture exists for Phase 3 to build on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> FastAPI `/api/*` | Untrusted request paths, methods and Accept headers cross here |
| browser -> FastAPI static `/` | Untrusted request paths reach the static file server |
| process env -> `config.py` | `.env` values including API keys enter the process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 | Spoofing | `app.frontend()` static fallback vs `/api/*` routes | medium | mitigate | Register `app.frontend()` with an absolute `directory` after every router; `test_api_not_shadowed` asserts the four-row Accept matrix so an API path can never silently return the SPA to a JSON client |
| T-1-03 | Information Disclosure | `GET /api/health` payload and startup logs | low | mitigate | Payload restricted to exactly four keys; `market_source` carries only the short name `simulator` or `massive`; `factory.py`'s existing rule of logging a key's presence and never its value is preserved in `config.py` |
| T-1-07 | Information Disclosure | path traversal through static serving | low | accept | `app.frontend()` delegates path resolution to Starlette; no hand-rolled path joining is introduced. Accepted at ASVS L1 for a localhost single-operator app |
| T-1-08 | Denial of Service | unbounded concurrent SSE connections | low | accept | Single local operator behind a single uvicorn worker (DOCK-06). Recorded in CONCERNS.md as a multi-user concern only |
| T-1-SC | Tampering | `uv add` / `uv sync` package installs | high | mitigate | RESEARCH.md `## Package Legitimacy Audit` covers all six packages: zero `[SLOP]`, zero flagged, all six named directly in PLAN.md or already declared, and all resolve, install and import. The researcher records explicitly that the `SUS` verdicts are artifacts of an unavailable downloads feed and that no human checkpoint is warranted |
</threat_model>

<assumption_delta_decision>
**Signal:** `pluralization` on the term "fallback", from "routes take precedence over the static frontend fallback."

**Noun now primary:** none — "fallback" here names a single Starlette behavior (serving `index.html` for an unmatched, HTML-accepting path), not an abstraction that has gained a second implementation.

**Decision:** `no-change`

**Rationale:** Confirming the orchestrator's read — this is a false positive. The one genuine singular-to-plural in this codebase is the pair of market data sources (simulator and Massive), and that is already correctly abstracted behind the `MarketDataSource` ABC in the frozen module, which this phase does not touch.
</assumption_delta_decision>

<flagged_assumptions>
**Probe-fallback accounting.** The phase's edge report carries 21 applicable rows, accounted as **11 covered + 1 backstop + 9 unresolved = 21**. The nine unresolved rows are the `unclassified` rows tabled below (SETUP-02, CORE-01, CORE-02, CORE-07), in plan 01-02 (CORE-04, CORE-05, CORE-10 — restated in plan 01-03, which owns the same requirement IDs and adds no new rows), and in plan 01-04 (SETUP-03, SETUP-04). The single backstop is the `app.frontend()` concurrency statement in this plan's `must_haves.truths`. The bucket totals are unchanged by this revision; what changed is *where* four of the eleven covered rows are recorded. CORE-08/`concurrency` and CORE-09/`concurrency` were claimed covered on the strength of the test name `test_concurrent_health_and_stream` buried in acceptance-criteria prose, and TEST-01/`boundary` and TEST-01/`precision` on the strength of a prohibitions entry. All four are now lifted into `must_haves.truths` — the first two in this plan, the second two in plan 01-03, which owns `insert_trade` — because the no-silent-drop rule counts a row as accounted only when it appears in `truths` or in a flagged assumption.

Probe rows that stayed `unresolved` because the edge probe could not classify the requirement. Each is surfaced rather than dropped, and none is auto-backstopped or auto-dismissed.

| Requirement | Probe category | Assumption carried |
|-------------|----------------|--------------------|
| SETUP-02 | unclassified | Raising the FastAPI floor to `>=0.141.1` has no behavioral edge beyond `app.frontend()` becoming available. RESEARCH.md verified `massive==2.2.0` imposes no FastAPI or pydantic constraint, so the bump is unobstructed — but no probe predicate was generated for it |
| CORE-01 | unclassified | The only ordering that matters in `create_app()` is cache-and-source before `include_router()`. The `app.frontend()` position was verified order-independent, so no further ordering edge is asserted |
| CORE-02 | unclassified | Lifespan enter/exit is assumed to run exactly once per app instance and `source.stop()` is assumed idempotent per the frozen module's docstring. No probe predicate covers a lifespan that raises mid-startup |
| CORE-07 | unclassified | Dependency injection is asserted through `app.state` identity and per-app distinctness. No probe predicate covers cache sharing across processes, which does not arise under the single-worker constraint |
</flagged_assumptions>

<artifacts_this_phase_produces>
New symbols and files created by this plan. Nothing here exists in the codebase before this plan runs.

**Files:** `backend/app/config.py`, `backend/app/api/__init__.py`, `backend/app/api/health.py`, `backend/app/main.py`, `backend/static/index.html`, `backend/.python-version`, `backend/tests/test_main.py`, `backend/tests/api/__init__.py`, `backend/tests/api/test_health.py`, `backend/tests/market/test_stream_integration.py`

**Functions:** `create_app`, `create_health_router`, `lifespan` (nested in `create_app`)

**Module constants:** `PROJECT_ROOT`, `DB_PATH`, `OPENROUTER_API_KEY`, `MASSIVE_API_KEY`, `LLM_MOCK` (in `config.py`); `STATIC_DIR` (in `main.py`)

**Environment variables:** `FINALLY_DB_PATH` (new, read by `config.py`)

**App state attributes:** `app.state.price_cache`, `app.state.market_source`, `app.state.db_path`

**Test symbols:** `test_spine_end_to_end`, `test_create_app_builds_cache_before_routers`, `test_lifespan_starts_and_stops_source`, `test_cache_via_dependency`, `test_api_not_shadowed`, `test_concurrent_health_and_stream`, `test_two_clients_stream_independently`, and the `app` / `db_path` conftest fixtures

**Dependency declarations:** `fastapi>=0.141.1`, `litellm>=1.95.0`, `pydantic>=2.10.0`, `python-dotenv>=1.0.0` (runtime); `httpx>=0.28.0` (dev extra)
</artifacts_this_phase_produces>

<verification>
- `cd backend && uv run --extra dev pytest -q` shows no new failures relative to the pre-phase baseline (D-24 gate wording — `tests/market/test_simulator.py::test_custom_update_interval` is a known pre-existing timing flake, not a regression from this work)
- `cd backend && uv run --extra dev ruff check app/ tests/` exits 0
- `cd backend && uv run --frozen python -c "import litellm, pydantic, dotenv, httpx"` exits 0
- `backend/app/market/` is unmodified: `git status --porcelain backend/app/market/` returns nothing
</verification>

<success_criteria>
- The assembled app answers `GET /api/health` with the four-key payload and streams `GET /api/stream/prices` with `retry: 1000`, price frames and heartbeats
- `/api/*` routes take precedence over the static frontend for JSON clients in all four Accept cases
- The market data source is started and stopped by lifespan, and `PriceCache` reaches the routers by injection with no module-level singleton
- `uv sync --frozen --extra dev` from the committed lockfile produces an environment where the chat dependencies import
</success_criteria>

<output>
Create `.planning/phases/01-foundation-spine/01-01-SUMMARY.md` when done
</output>
