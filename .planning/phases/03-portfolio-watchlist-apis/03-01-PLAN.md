---
phase: 03-portfolio-watchlist-apis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/__init__.py
  - backend/app/services/errors.py
  - backend/app/services/portfolio.py
  - backend/app/services/trading.py
  - backend/app/api/models.py
  - backend/app/api/errors.py
  - backend/app/api/portfolio.py
  - backend/app/api/__init__.py
  - backend/app/main.py
  - backend/tests/services/__init__.py
  - backend/tests/services/test_trading.py
  - backend/tests/api/test_portfolio.py
autonomous: false
requirements: [PORT-02, PORT-07, PORT-10, PORT-11]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A POST to /api/portfolio/trade with a valid buy returns 200 and a body whose fill_price is the price the server read from the cache, not a price the client sent"
    - "After a successful buy, cash_balance has decreased by fill_price times quantity and a positions row exists for the ticker at the paid avg_cost"
    - "Buying a ticker that was not on the watchlist leaves that ticker on the watchlist (PORT-07)"
    - "Every executed trade leaves exactly one new portfolio_snapshots row, written inside the same transaction as the cash and position writes (PORT-11)"
    - "A buy costing exactly the whole cash balance succeeds: the sufficiency test compares round_money(cost) against round_money(cash), so equality is not a rejection (PORT-02 boundary)"
    - "Cash and quantity reach the database as raw computed values and are rounded once at the queries.py write boundary; the service never calls round_money or round_quantity on a value it stores (PORT-02 precision)"
    - "fill_price in the response is the exact float returned by wait_for_price, captured before the transaction opens and never re-read from the cache afterwards (PORT-10 boundary)"
    - "fill_price and total_cost are returned at full float precision with no server-side rounding of the derived figure (PORT-10 precision)"
    - "A trade whose post-trade total equals the previous snapshot's total still writes a snapshot row; the unchanged-value skip belongs to the 30-second task only (PORT-11 adjacency)"
    - "A first trade against a freshly seeded database writes its snapshot even though only the seed snapshot exists (PORT-11 empty)"
    - "A trade rejected inside the transaction leaves no snapshot row, no watchlist row and no cash change, because writing() rolls the whole unit back (PORT-11 ordering)"
  prohibitions:
    - requirement_id: PORT-02
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT enforce any trade or watchlist rule only at the HTTP boundary. Every quantity, ticker, side, cash and shares rule lives behind the service seam so a non-HTTP caller (the Phase 6 LLM path) cannot bypass what a manual request is held to."
  artifacts:
    - path: "backend/app/services/errors.py"
      provides: "TradeError, NotFound and Conflict, all subclassing ValueError"
      contains: "class TradeError"
    - path: "backend/app/services/trading.py"
      provides: "execute_trade seam, _apply_trade unit of work, validate_quantity"
      contains: "async def execute_trade"
    - path: "backend/app/services/portfolio.py"
      provides: "value_portfolio pure valuation function"
      contains: "def value_portfolio"
    - path: "backend/app/api/errors.py"
      provides: "register_exception_handlers mapping the taxonomy onto 400/404/409"
      contains: "def register_exception_handlers"
    - path: "backend/app/api/portfolio.py"
      provides: "create_portfolio_router with POST /api/portfolio/trade"
      contains: "def create_portfolio_router"
    - path: "backend/app/api/models.py"
      provides: "Pydantic request and response models for every Phase 3 route"
      contains: "class TradeRequest"
    - path: "backend/tests/services/test_trading.py"
      provides: "End-to-end proof of the happy buy through the seam"
    - path: "backend/tests/api/test_portfolio.py"
      provides: "Route-level proof that the response carries the server-side fill_price, and that a rejected trade reaches the client as HTTP 400 with a readable detail"
  key_links:
    - from: "backend/app/api/portfolio.py"
      to: "backend/app/services/trading.py"
      via: "the route awaits execute_trade(db_path, cache, ...) and never touches sqlite3 itself"
      pattern: "execute_trade\\("
    - from: "backend/app/services/trading.py"
      to: "backend/app/db/connection.py"
      via: "await run_db(db_path, _apply_trade, ...) with writing(conn) inside"
      pattern: "run_db\\("
    - from: "backend/app/main.py"
      to: "backend/app/api/portfolio.py"
      via: "include_router(create_portfolio_router(cache)) registered before app.frontend()"
      pattern: "create_portfolio_router"
---

<objective>
Prove the whole Phase 3 architecture on one path: a single successful buy travelling
router to service to `run_db` to one `BEGIN IMMEDIATE` transaction and back out as a
response carrying the server-side `fill_price` — with its test green and committed.

Purpose: every remaining plan in this phase (rejection branches, watchlist, history,
reset, the snapshot task) is expansion on top of this seam. Proving the seam first means
an architectural dead-end costs one commit, not ten. This plan also publishes the two
contracts later work depends on: the `execute_trade` signature Phase 6 must route
through, and the complete Pydantic model surface that lets the wave-2 plans run in
parallel without fighting over one file.

Output: `app/services/` as a real package with the error taxonomy, the pure valuation
function and the trade seam; `app/api/` with the exception handlers, the full model
surface and a portfolio router carrying `POST /api/portfolio/trade`; `main.py` wired;
two tests proving the path end to end.
</objective>

<execution_context>
@C:/Users/ehasi/OneDrive/Documents/AI Coder Course/Project/finally/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/ehasi/OneDrive/Documents/AI Coder Course/Project/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-portfolio-watchlist-apis/03-CONTEXT.md
@.planning/phases/03-portfolio-watchlist-apis/03-RESEARCH.md
@.planning/phases/03-portfolio-watchlist-apis/03-PATTERNS.md
@planning/PLAN.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Publish the service seam signatures</name>
  <reversibility rating="one-way">`.planning/ROADMAP.md` records these functions as the contract Phase 6 (CHAT-07/08/09) is planned against; changing the shape after Phase 6 lands breaks a cross-phase contract, not just a call site.</reversibility>
  <decision>The exact published signatures of `services.trading.execute_trade` and `services.watchlist.add` / `services.watchlist.remove`. CONTEXT.md D-03 fixes `execute_trade`; the two watchlist functions have no signature recorded anywhere, and Phase 6 will be planned against whatever this task writes down.</decision>
  <context>These three functions are the only doors between the HTTP layer and the business rules. The Phase 6 LLM path calls them directly with no FastAPI object in hand, so their parameter list is a published contract. Undoing a change later means replanning Phase 6 and rewriting both call sites, which is why this is a one-way door rather than a rename.</context>
  <options>
    <option id="option-a">
      <name>Publish as specified, watchlist mirroring the trade shape</name>
      <pros>`async def execute_trade(db_path: Path, cache: PriceCache, ticker: str, side: str, quantity: float) -> TradeResult` is D-03 verbatim. `async def add(db_path: Path, source: MarketDataSource, ticker: str) -> WatchlistEntry` and `async def remove(db_path: Path, ticker: str) -> None` follow the same rule: path first, collaborators next, payload last, no FastAPI object crosses. Every argument is visible at the call site, and Phase 6 passes exactly what the router passes.</pros>
      <cons>`remove` takes no collaborator while `add` does, so the two are not symmetric.</cons>
    </option>
    <option id="option-b">
      <name>Symmetric watchlist pair — both take the source</name>
      <pros>`add` and `remove` have identical parameter lists, so a caller holding a source can call either without checking.</pros>
      <cons>`remove` would accept a collaborator it never uses (D-08 removes only the database row; the simulator keeps producing a price for an unwatched ticker until restart, which is harmless). An unused parameter is a lie about the dependency.</cons>
    </option>
    <option id="option-c">
      <name>Adjust before locking</name>
      <pros>Anything the developer wants changed is free to change now and expensive to change after Phase 6 is planned.</pros>
      <cons>Diverges from D-03, which the developer already locked.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c (describe the adjustment)</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: End-to-end buy — one path only</name>
  <reversibility rating="one-way">Implements the seam signatures approved in Task 1; the `execute_trade` parameter list becomes the published Phase 6 contract the moment this lands.</reversibility>
  <files>backend/app/services/__init__.py, backend/app/services/errors.py, backend/app/services/portfolio.py, backend/app/services/trading.py, backend/app/api/errors.py, backend/app/api/portfolio.py, backend/app/api/models.py, backend/app/main.py, backend/tests/services/__init__.py, backend/tests/services/test_trading.py, backend/tests/api/test_portfolio.py</files>
  <read_first>
    - backend/app/db/connection.py — `run_db(path, fn, *args)` at line 137, `writing(conn)` at line 74, `get_db_path(request)` at line 125. These are the seams; do not add a second one.
    - backend/app/db/queries.py — `get_profile` (39), `update_cash_balance` (47), `get_positions` (65), `get_position` (78), `upsert_position` (89), `insert_trade` (139, returns `executed_at`), `insert_snapshot` (178), `add_watchlist_ticker` (246). Read the SELECT lists for the exact row column names.
    - backend/app/db/money.py — `round_money` (33), `round_quantity` (46), `is_zero` (56) and the module docstring at lines 9-19, which forbids rounding a derived value.
    - backend/app/market/cache.py — `wait_for_price` (145) and the `ValueError` it raises at line 157; `get_all` (101); `update` (38).
    - backend/app/market/tickers.py — `normalize_ticker` (10) and the `ValueError` it raises at line 21.
    - backend/app/api/health.py — the whole file: the `create_X_router(...)` factory convention this router copies.
    - backend/app/main.py — `create_app()` lines 23-55, especially that `app.frontend(...)` is the last call before `return app`.
    - backend/tests/api/test_health.py — the route-test shape, including `app.state.price_cache.update("AAPL", 190.50)` at line 42 as the way to give a route a price without running lifespan.
    - backend/tests/db/test_queries.py lines 1-64 — the local `conn` fixture over a real file database under `tmp_path`.
    - backend/tests/conftest.py — the `db_path` and `app` fixtures this plan reuses verbatim.
    - .planning/phases/03-portfolio-watchlist-apis/03-RESEARCH.md — Pattern 2, Pattern 3, the Code Examples section, and Pitfalls 2, 3, 4, 7 and 8.
  </read_first>
  <action>
Wire one entry point — a single successful buy — through every layer this phase touches. No other route, no rejection branches beyond the two the transaction structurally needs, no watchlist module.

Create `backend/app/services/errors.py`: `TradeError`, `NotFound` and `Conflict`, each subclassing `ValueError` (D-06), each with a docstring body naming the status it maps to and no other members. Nothing under `backend/app/services/` may import FastAPI's HTTP error type — the taxonomy is the only signalling mechanism (D-07).

Create `backend/app/services/portfolio.py` with the pure function `value_portfolio(cash: float, positions: list[sqlite3.Row], prices: dict[str, float]) -> tuple[list[dict[str, object]], float]` (D-18). Per position read `ticker`, `quantity`, `avg_cost`. When `prices.get(ticker)` is `None`, emit `current_price`, `market_value`, `unrealized_pnl` and `unrealized_pnl_percent` as `None` and leave that position out of the running total (D-14). Otherwise `market_value = quantity * price`, `cost_basis = quantity * avg_cost`, `unrealized_pnl = market_value - cost_basis`, `unrealized_pnl_percent = (market_value - cost_basis) / cost_basis * 100`, and add `market_value` to the total which starts at `cash`. Return every derived figure unrounded. No I/O, no cache, no connection — the trade transaction calls this from inside an executor thread.

Create `backend/app/services/trading.py`. Define `TradeResult` as a frozen dataclass with `ticker`, `side`, `quantity`, `fill_price`, `total_cost`, `cash_balance`, `executed_at`. Define `validate_quantity(quantity: float) -> float` raising `TradeError`, checking in this fixed order: `math.isfinite`, then greater than zero, then `round_quantity(quantity) == quantity`. The order is load-bearing — `round(float("inf"), 4)` returns `inf`, so a precision check placed first lets an infinite quantity through (RESEARCH Pitfall 3). Define `async def execute_trade(...)` with the signature approved in Task 1. Its body runs cheap checks, then price, then balance (D-04): `normalize_ticker(ticker)`; a side check accepting only the two lowercase literals the schema stores; `validate_quantity(quantity)`; then `fill_price = await wait_for_price(cache, ticker, timeout=2.0)`; then capture `prices = {t: u.price for t, u in cache.get_all().items()}` before the transaction opens (D-02); then `await run_db(db_path, _apply_trade, ticker, side, quantity, fill_price, prices)` and build the `TradeResult` from what comes back.

Define `def _apply_trade(conn: sqlite3.Connection, ticker: str, side: str, quantity: float, fill_price: float, prices: dict[str, float]) -> dict[str, object]` — a plain `def` taking the connection first, matching every function in `queries.py`. Everything happens inside one `with writing(conn):` block (D-01), in this order: `add_watchlist_ticker(conn, ticker)` first, so a trade that is then rejected rolls the row back with everything else (PORT-07, RESEARCH Pitfall 7); `get_profile(conn)`; `get_position(conn, ticker)`; the buy sufficiency guard comparing `round_money(fill_price * quantity)` against `round_money(profile["cash_balance"])` and raising `TradeError` when cost exceeds cash, with the message shaped exactly like PLAN.md section 8's worked example (need amount then have amount, both at two decimal places); `update_cash_balance(conn, cash - cost)`; `upsert_position(conn, ticker, new_qty, new_avg)` where `new_avg = (old_qty * old_avg + quantity * fill_price) / new_qty` for a buy (D-05) and `old_qty`/`old_avg` are zero when there is no existing row; `executed_at = insert_trade(conn, ticker, side, quantity, fill_price)`; then re-read `get_positions(conn)`, call `value_portfolio(new_cash, rows, prices)` and `insert_snapshot(conn, total_value)` (PORT-11). Pass raw values to every query function — rounding happens once at the write boundary and `upsert_position` deliberately rounds `avg_cost` with `round_quantity` (RESEARCH Pitfall 8; do not change it, do not pre-round). Return a plain dict carrying `executed_at`, the new `cash_balance` and the cost.

The sell branch is out of scope for this task, and the stub's **location is pinned** because 03-02 replaces it in place: the stub lives inside `_apply_trade`, inside the `with writing(conn):` block, at the same branch point on `side` where the buy cash sufficiency guard sits — not in `execute_trade`'s pre-flight checks and not above the transaction. When `side` is `sell`, raise `TradeError` naming sells as not yet wired. This is the one stub, and 03-02 fills it by replacing that single line with the sell arm beside the buy arm, so the transaction's shape never changes. A stub placed anywhere else silently invalidates 03-02 Task 1's instructions, which describe the code they expect to find at that branch point.

Create `backend/app/api/errors.py` with `register_exception_handlers(app: FastAPI) -> None` registering four handlers, each returning `JSONResponse({"detail": str(exc)}, status_code=...)`: `Conflict` to 409, `NotFound` to 404, `TradeError` to 400, and bare `ValueError` to 400. The bare-`ValueError` row is load-bearing, not belt-and-braces: `normalize_ticker` and `wait_for_price` both raise plain `ValueError`, and without it an invalid symbol and a price timeout both become 500s. Starlette resolves handlers by walking the exception's MRO and taking the first registered class, so the subclasses stay correctly mapped.

Create `backend/app/api/models.py` with the two models this path needs: `TradeRequest` declaring `ticker: str`, `side: str`, `quantity: float` and nothing else — no `gt`, no constraints, no validators (D-20: a Pydantic constraint returns 422 where PLAN.md section 8 requires 400, and the Phase 6 path never builds this model) — and `TradeResponse` declaring `ticker: str`, `side: str`, `quantity: float`, `fill_price: float`, `total_cost: float`, `cash_balance: float`, `executed_at: str`. Task 3 fills in the rest of the surface.

Create `backend/app/api/portfolio.py` with `create_portfolio_router(price_cache: PriceCache) -> APIRouter`, copying the shape of `create_health_router`: module docstring, `from __future__ import annotations`, `APIRouter(prefix="/api", tags=["portfolio"])` built inside the factory, `return router` last. One route for now: `@router.post("/portfolio/trade", response_model=TradeResponse)` whose handler takes `body: TradeRequest` and `db_path: Annotated[Path, Depends(get_db_path)]`, awaits `execute_trade`, and returns the result. The handler catches nothing — the app-level handlers own translation.

Edit `backend/app/main.py`: import `create_portfolio_router` and `register_exception_handlers`; call `register_exception_handlers(app)` after the `FastAPI(...)` construction; add `app.include_router(create_portfolio_router(cache))` alongside the existing `include_router` calls and above the `app.frontend(...)` line, which must remain the last call before `return app`. Edit `backend/app/api/__init__.py` to add `create_portfolio_router` to both the Public API docstring list and `__all__` (that file has no `from __future__` line — match it).

Create `backend/app/services/__init__.py` in the shape of `backend/app/db/__init__.py`: a Public API docstring listing each export with a one-line description, explicit re-imports, then an alphabetically sorted `__all__`. Export only what exists after this plan: `Conflict`, `NotFound`, `TradeError`, `TradeResult`, `execute_trade`, `validate_quantity`, `value_portfolio`. Later plans extend it.

Create `backend/tests/services/__init__.py` as a bare package marker copying `backend/tests/api/__init__.py`.

Create `backend/tests/services/test_trading.py` with a local `conn`-style setup where useful and one end-to-end async test proving the happy path: seed a `PriceCache`, `cache.update("PYPL", 100.0)`, call `execute_trade` against a `tmp_path` database seeded with `apply_schema` + `seed_fresh`, and assert in one test that the returned `fill_price` is `100.0`, the returned `cash_balance` is `10000.0 - 100.0 * quantity`, a `positions` row exists at the paid `avg_cost`, `PYPL` is now on the watchlist even though the seed list does not contain it, and exactly one snapshot row was added beyond the seed snapshot. Use `STARTING_CASH` from `app.db.seed` rather than the literal.

Create `backend/tests/api/test_portfolio.py` with two route tests in the `test_health.py` shape.

The first proves the happy path: `app.state.price_cache.update("AAPL", 190.52)`, `TestClient(app).post("/api/portfolio/trade", json=...)`, assert 200 and that the response body's `fill_price` equals `190.52` regardless of any price the client might have believed.

The second proves that a trade rejection reaches the client as a readable 400 rather than a 500 carrying a Starlette traceback body. This is the observable contract PLAN.md section 8 and D-07 promise, and it is the only test in the phase that exercises it for the trade path — 03-04's watchlist route tests cover the bare-`ValueError`, `Conflict` and `NotFound` rows, and nothing else covers a `TradeError` crossing the HTTP boundary. Seed the same price, then POST a buy whose quantity the seeded starting cash cannot cover, deriving that quantity from `STARTING_CASH` and the seeded price rather than hard-coding a dollar figure. Assert `status_code == 400`, that the body carries a `detail` key, and that `detail` matches PLAN.md section 8's worked shape — the word Insufficient, the needed dollar figure at two decimal places, then the available figure at two decimal places. Assert the message text, not the status alone: `detail` is what the client renders verbatim, and a status-only assertion would pass against an empty or a repr-shaped body. `register_exception_handlers` is what makes this pass; whichever registered row resolves it, the same one call wires them all, so a rejection that surfaces as a 500 here means the registration itself is wrong.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run --extra dev pytest tests/services/test_trading.py tests/api/test_portfolio.py tests/test_main.py -q</automated>
    <automated>cd backend &amp;&amp; uv run --extra dev ruff check app/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run --extra dev pytest tests/services/test_trading.py tests/api/test_portfolio.py -q` exits 0 with at least 3 tests passing
    - `cd backend && uv run --extra dev pytest tests/test_main.py -q` exits 0 — the existing `test_api_not_shadowed` guard still passes, proving the new router did not disturb the static mount order
    - `cd backend && uv run --extra dev ruff check app/ tests/` exits 0
    - `backend/app/services/trading.py` contains `async def execute_trade(` and `def _apply_trade(`
    - `backend/app/services/trading.py` contains `with writing(conn):` exactly once, and every one of `add_watchlist_ticker`, `update_cash_balance`, `upsert_position`, `insert_trade`, `insert_snapshot` appears after it
    - `grep -rn "HTTPException" backend/app/services/ | wc -l` outputs `0`
    - `grep -rn "BEGIN IMMEDIATE\|INSERT INTO\|SELECT " backend/app/services/ | wc -l` outputs `0` — the service layer composes existing query functions and writes no SQL
    - `backend/app/api/errors.py` registers exactly four handlers, one of which is keyed on `ValueError`
    - `backend/tests/api/test_portfolio.py` contains a test that POSTs a buy the seeded starting cash cannot cover and asserts `status_code == 400` together with a `detail` matching PLAN.md section 8's Insufficient-cash shape — the phase's only proof that a trade rejection reaches the client as a readable 400 rather than a 500 (D-07)
    - In `backend/app/services/trading.py` the `TradeError` raised when `side` is `sell` appears **after** the `with writing(conn):` line and beside the buy cash guard, inside `_apply_trade` — not in `execute_trade` — so 03-02 can replace that single line in place
    - In `backend/app/main.py` the `app.frontend(` call is the last statement before `return app`, appearing after every `include_router` call
    - `backend/app/services/portfolio.py` contains `def value_portfolio(` and the function body contains no `await`, no `run_db`, and no reference to a cache
    - A trade rejected by the cash guard leaves the database unchanged: `pytest tests/services/test_trading.py -q` includes an assertion that a rolled-back trade added no watchlist row
  </acceptance_criteria>
  <done>`POST /api/portfolio/trade` executes a real buy end to end against a real SQLite file, returns the server-side `fill_price`, and the whole path is committed with its tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the full Pydantic model surface</name>
  <files>backend/app/api/models.py</files>
  <read_first>
    - backend/app/api/models.py — the two models Task 2 created; extend, do not rewrite.
    - planning/PLAN.md section 8 — the verbatim response shapes for `GET /api/portfolio`, `GET /api/portfolio/history` and `GET /api/watchlist`.
    - .planning/phases/03-portfolio-watchlist-apis/03-CONTEXT.md — D-14, D-15, D-19, D-20.
    - backend/app/db/queries.py — the SELECT lists at lines 65, 194, 210 and 234 for the exact snapshot and watchlist column names.
  </read_first>
  <action>
Declare the remaining Pydantic models so the wave-2 plans build their routes against a file that is already finished, and none of them has to edit this file (which would serialize three otherwise parallel plans).

Add to `backend/app/api/models.py`, shape only, no constraints and no validators (D-19, D-20):

`PositionOut` — `ticker: str`, `quantity: float`, `avg_cost: float`, `current_price: float | None`, `market_value: float | None`, `unrealized_pnl: float | None`, `unrealized_pnl_percent: float | None`. The four nullable fields are D-14's null rule made explicit in the OpenAPI schema.

`PortfolioResponse` — `cash_balance: float`, `total_value: float`, `positions: list[PositionOut]`.

`SnapshotOut` — `total_value: float`, `recorded_at: str`. `HistoryResponse` — `snapshots: list[SnapshotOut]`.

`WatchlistTickerOut` — `ticker: str`, `price: float | None`, `open_price: float | None`, `change_from_open_percent: float | None`, `history: list[float]`. Three nullable fields plus an empty list are D-15's null rule; a ticker with no price is present in the response with nulls, never omitted and never zeroed.

`WatchlistResponse` — `tickers: list[WatchlistTickerOut]`. `WatchlistAddRequest` — `ticker: str` and nothing else.

Timestamps are ISO 8601 UTC strings exactly as `queries.py` stored them, typed `str` — do not reformat and do not type them as `datetime`, which would make Pydantic re-serialize and change the string the client sees.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run --extra dev pytest tests/api -q</automated>
    <automated>cd backend &amp;&amp; uv run --extra dev python -c "from app.api.models import PortfolioResponse, HistoryResponse, WatchlistResponse, WatchlistAddRequest, PositionOut, SnapshotOut, WatchlistTickerOut; print(sorted(PositionOut.model_fields))"</automated>
  </verify>
  <acceptance_criteria>
    - The import check above exits 0 and prints `['avg_cost', 'current_price', 'market_value', 'quantity', 'ticker', 'unrealized_pnl', 'unrealized_pnl_percent']`
    - `backend/app/api/models.py` declares all nine model classes: `TradeRequest`, `TradeResponse`, `PositionOut`, `PortfolioResponse`, `SnapshotOut`, `HistoryResponse`, `WatchlistTickerOut`, `WatchlistResponse`, `WatchlistAddRequest`
    - `grep -c "gt=\|ge=\|le=\|field_validator\|model_validator" backend/app/api/models.py` outputs `0` — every business rule lives in the service layer
    - `cd backend && uv run --extra dev ruff check app/` exits 0
    - `cd backend && uv run --extra dev pytest -q` collects at least 245 tests and reports no failures other than the known `test_custom_update_interval` flake
  </acceptance_criteria>
  <done>Every Phase 3 response shape is declared and importable, so plans 03-02, 03-03 and 03-04 can run in parallel without touching this file.</done>
</task>

</tasks>

## Artifacts this phase produces

New symbols created by this plan (excluded from drift verification — none of these existed before):

| Symbol | Kind | File |
|---|---|---|
| `TradeError`, `NotFound`, `Conflict` | exception classes | `backend/app/services/errors.py` |
| `value_portfolio` | function | `backend/app/services/portfolio.py` |
| `TradeResult` | frozen dataclass | `backend/app/services/trading.py` |
| `validate_quantity` | function | `backend/app/services/trading.py` |
| `execute_trade` | async function (published seam) | `backend/app/services/trading.py` |
| `_apply_trade` | function | `backend/app/services/trading.py` |
| `register_exception_handlers` | function | `backend/app/api/errors.py` |
| `create_portfolio_router` | router factory | `backend/app/api/portfolio.py` |
| `TradeRequest`, `TradeResponse`, `PositionOut`, `PortfolioResponse`, `SnapshotOut`, `HistoryResponse`, `WatchlistTickerOut`, `WatchlistResponse`, `WatchlistAddRequest` | Pydantic models | `backend/app/api/models.py` |
| `backend/app/services/__init__.py`, `backend/tests/services/__init__.py` | new package markers | — |
| `backend/tests/services/test_trading.py`, `backend/tests/api/test_portfolio.py` | new test modules | — |

## Flagged assumptions (unresolved probe items)

The edge probe returned one `unclassified` row against this plan's requirements. It is
recorded here rather than silently dropped, and stays `unresolved`:

- **PORT-07 — unclassified.** "Trading a ticker that is not on the watchlist adds it to the
  watchlist as part of the trade" produced no edge category the probe could classify. The
  planner's reading, which the executor implements: *as part of* means inside the same
  `writing()` block, so a rejected trade adds nothing. Not auto-backstopped; a reviewer
  should confirm that reading.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to `/api/*` | Untrusted JSON body crosses here — ticker string, side string, quantity float |
| service to SQLite (`run_db` / `writing`) | Untrusted values reach parameterized SQL and money arithmetic |
| service to price cache | The service reads prices; it never lets the client name one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `ticker` in `TradeRequest` reaching SQL and the market source | medium | mitigate | `normalize_ticker` (`^[A-Z]{1,5}$`) before any use; `queries.py` binds `?` placeholders exclusively and this plan writes zero SQL |
| T-03-02 | Tampering | `quantity` float corrupting `cash_balance` with `NaN` or `Infinity` | high | mitigate | `validate_quantity` runs `math.isfinite` before the positivity and precision checks, in the service, before any arithmetic (proven necessary: `round(inf, 4)` returns `inf`) |
| T-03-03 | Tampering | Two concurrent buys both passing the cash check (TOCTOU double-spend) | high | mitigate | The whole read-modify-write is one composed function inside `writing(conn)` = `BEGIN IMMEDIATE`, with `busy_timeout=5000` absorbing contention |
| T-03-05 | Info disclosure | Error bodies shown to the user verbatim | medium | mitigate | Handlers emit `{"detail": str(exc)}` where `exc` carries a service-authored message; never a repr, never a filesystem path, never a traceback |
| T-03-06 | Elevation of privilege | `user_id` becoming a request-controlled parameter | medium | mitigate | No route, model or service signature accepts `user_id`; every query call takes the `DEFAULT_USER_ID` default |
| T-03-07 | Denial of service | Blocking the event loop and stalling the SSE stream | medium | mitigate | All database work goes through `run_db`'s `asyncio.to_thread`; no synchronous `sqlite3` call is added to a route |
| T-03-08 | Tampering | A rejected trade leaving partial state (cash debited with no position, orphan watchlist row) | high | mitigate | Every write is inside one `writing()` block; raising inside it rolls back and re-raises |
| T-03-SC | Tampering | Package-manager installs | informational | accept | This phase installs zero packages; `backend/pyproject.toml` and `backend/uv.lock` are not edited, so there is no supply-chain surface to audit. Recorded at `informational` rather than `high` because the threat has no surface in this phase — a `high` with disposition `accept` would trip the phase's own block-on-`high` rule for a risk that does not exist |
</threat_model>

<verification>
- `cd backend && uv run --extra dev pytest -q` — at least 245 tests, no failures beyond the one known `tests/market/test_simulator_source.py::TestSimulatorDataSource::test_custom_update_interval` flake (pre-existing, owned by the frozen market module, must not be chased)
- `cd backend && uv run --extra dev ruff check app/ tests/` exits 0
- `git status` shows `db/finally.db` unmodified after the test run
</verification>

<success_criteria>
- A buy placed against `POST /api/portfolio/trade` fills at the server's price, debits cash, creates the position, adds the ticker to the watchlist and writes a snapshot — all in one transaction
- `execute_trade`'s signature is the one the developer approved in Task 1
- Every Phase 3 response shape is declared in `app/api/models.py`
- No plan file, service module or router contains SQL, and no service imports a FastAPI exception type
</success_criteria>

<output>
Create `.planning/phases/03-portfolio-watchlist-apis/03-01-SUMMARY.md` when done
</output>
