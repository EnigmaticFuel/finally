---
context: default
phase: 4-frontend-shell
task: null
total_tasks: null
status: not_started
last_updated: 2026-08-15T19:25:39.310Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| GSD commit verb no-ops on untracked paths | `gsd-tools query commit --files <path>` returns success-shaped JSON while committing nothing when the named path is untracked. Both handoff files here were new/untracked. | advisory | After any `query commit`, run `git log -1 --stat` and confirm the file actually landed; fall back to plain `git add` + `git commit` if it did not. |
| Worktree cleanup stalls on OneDrive | Worktree removal blocks the wave loop after a successful merge, stranding later plans as `pending`. | advisory | `git worktree list` currently shows only the main checkout - clean. If Phase 4 runs parallel plans in worktrees, check for stranded `pending` plans before assuming a wave failed. |

<current_state>
Clean phase boundary. Phase 3 is fully closed and Phase 4 has not started.

- No `.planning/phases/04-*` directory exists yet.
- Working tree is clean apart from `.claude/settings.local.json` (modified, unrelated to phase work).
- HEAD is `d3bfab4` on branch `finally-gsd` — that commit deliberately removed the Phase 3 handoff after completion.
- No async jobs, no running containers, no stale worktrees.

Nothing is half-finished. This handoff records the boundary, not an interruption.
</current_state>

<completed_work>

- Phase 01 — Foundation & Spine: 5/5 plans. Assembled FastAPI app, lazy DB seed, live price stream, `/api/*` precedence over the static fallback.
- Phase 02 — Walking Skeleton & Container: 4/4 plans. Single-origin serving proven; `.sh` pair validated on Ubuntu WSL2.
- Phase 03 — Portfolio & Watchlist APIs: 9/9 plans. UAT 4/4 passed, `03-VERIFICATION.md` status `passed`, `03-SECURITY.md` `threats_open: 0` (57 threats, 26 audited with file:line evidence), all 21 phase-3 requirement IDs complete.

Progress: 3/7 phases, 18/18 plans complete.
</completed_work>

<remaining_work>

- **Phase 4 — Frontend Shell** (next): UI-01 through UI-16. Dark terminal, watchlist panel, header with cash / live total / total return vs $10k, trade bar, connection status dot, price flash.
- Phase 5 — Charts & Visualization
- Phase 6 — AI Chat Copilot
- Phase 7 — Production Container & Full Verification
</remaining_work>

<decisions_made>

Carried into Phase 4 (from PROJECT.md, ROADMAP.md, `03-SECURITY.md`):

- **Error bodies render as text, never markup.** `{"detail": ...}` echoes the user's submitted string back — Phase 3 accepted risk AR-03-03.
- **Flash by tick; colour and change column by change-from-open**, labeled "Chg from Open" rather than "Change %".
- **Exactly one `EventSource`**, surviving a React StrictMode remount without leaking a second. No polling, no second live channel.
- **Seam with Phase 5:** UI-03 delivers the watchlist row and its plumbing; the sparkline filling its chart slot is CHART-02. UI-14 delivers selection state; the main chart consuming it is CHART-01. UI-13's refetch rule is written here and extended to chat in Phase 6.
- Three approved additions beyond PLAN.md: Reset Portfolio (PORT-14), visible fill confirmation (UI-09), total return vs $10k (UI-11).
- `app.frontend()` over ordered `StaticFiles`; Node 24.
</decisions_made>

<blockers>
- **Unverified (Phase 6, not Phase 4):** live structured-output behaviour through OpenRouter → Cerebras. Spike before writing the chat router.
- **Accepted risk, not a task:** the project lives inside OneDrive and `db/finally.db` is tracked in git. If `database is locked` appears, that is the cause — diagnose it, but do not plan a relocation or an untracking.
- **[Phase 6] The `api-coverage` gate was overridden as a false positive for Phase 1.** Phase 6 is the first phase with genuine external-API integration; honor the gate there.
</blockers>

## Required Reading (in order)

1. `.planning/STATE.md` — current position, accumulated decisions, blockers
2. `.planning/ROADMAP.md` lines 140-157 — Phase 4 goal, requirements UI-01..UI-16, five success criteria, and the Phase 5 seam
3. `.planning/REQUIREMENTS.md` — the UI-01..UI-16 requirement text
4. `.planning/phases/03-portfolio-watchlist-apis/03-SEAM-CONTRACT.md` — the API shapes Phase 4 consumes
5. `.planning/phases/03-portfolio-watchlist-apis/03-SECURITY.md` — AR-03-03, the render-as-text obligation inherited by the frontend
6. `planning/PLAN.md` sections 8 and 10 — endpoint contracts and frontend design

## Infrastructure State

- **Branch:** `finally-gsd` at `d3bfab4`. Clean apart from `.claude/settings.local.json`.
- **Docker:** no `finally` container running.
- **Worktrees:** none registered beyond the main checkout.
- **Backend:** `backend/` is a working uv project; 154+ market-data tests plus phase 1-3 suites pass.
- **Frontend:** `frontend/` is still empty — no `package.json`, no source. Phase 4 creates it.
- **Async jobs:** none.

<context>
Phase 3 was closed deliberately and completely — UAT, verification, and security all signed
off, and the previous handoff was removed on purpose because there was nothing left to hand
off. This pause is a bookmark at the start of Phase 4, not a rescue.

Phase 4 is the first phase that writes any frontend code. It builds against the settled
API shapes from Phase 3 (that was the point of doing the gap-closing work before starting
here), so the plumbing questions are already answered. The open questions are visual and
structural: layout, the empty states every panel needs on first paint, and where the single
EventSource lives so StrictMode cannot double it.

ROADMAP marks Phase 4 with **UI hint: yes**, so `/gsd-ui-phase 4` belongs in the sequence
between discussion and planning.
</context>

<next_action>
Start with: `/gsd-discuss-phase 4` — gather context for the Frontend Shell. Then `/gsd-ui-phase 4`
to produce the UI-SPEC design contract, then `/gsd-plan-phase 4`.
</next_action>
