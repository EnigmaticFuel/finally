{
  "name": "finally-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end suite. Runs on the host against the container started by scripts/start_windows.ps1.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "@types/node": "^22.10.0"
  }
}
